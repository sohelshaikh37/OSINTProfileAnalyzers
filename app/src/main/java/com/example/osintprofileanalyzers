package com.example.osintprofileanalyzer;

import android.os.Bundle;
import android.text.TextUtils;
import android.widget.Button;
import android.widget.EditText;
import android.widget.LinearLayout;
import android.widget.ScrollView;
import android.widget.TextView;

import androidx.appcompat.app.AppCompatActivity;

public class MainActivity extends AppCompatActivity {

EditText usernameInput;
TextView resultText;

@Override
protected void onCreate(Bundle savedInstanceState) {
    super.onCreate(savedInstanceState);

    LinearLayout layout = new LinearLayout(this);
    layout.setOrientation(LinearLayout.VERTICAL);
    layout.setPadding(40, 60, 40, 40);

    TextView title = new TextView(this);
    title.setText("OSINT PROFILE ANALYZER");
    title.setTextSize(26);

    TextView subtitle = new TextView(this);
    subtitle.setText(
            "Public-source username analyzer\n"
            + "No login • No private access • No bypass"
    );
    subtitle.setTextSize(16);
    subtitle.setPadding(0, 15, 0, 25);

    usernameInput = new EditText(this);
    usernameInput.setHint("Enter username");
    usernameInput.setSingleLine(true);

    Button analyzeButton = new Button(this);
    analyzeButton.setText("ANALYZE");

    resultText = new TextView(this);
    resultText.setTextSize(16);
    resultText.setPadding(0, 25, 0, 25);

    ScrollView scrollView = new ScrollView(this);
    scrollView.addView(resultText);

    layout.addView(title);
    layout.addView(subtitle);
    layout.addView(usernameInput);
    layout.addView(analyzeButton);
    layout.addView(scrollView);

    setContentView(layout);

    analyzeButton.setOnClickListener(v -> analyzeUsername());
}

private void analyzeUsername() {

    String username = usernameInput.getText().toString().trim();

    if (TextUtils.isEmpty(username)) {
        resultText.setText("Please enter a username.");
        return;
    }

    String clean = username.replace("@", "");

    String report =
            "PUBLIC PROFILE / OSINT REPORT\n"
            + "==============================\n\n"

            + "TARGET\n"
            + clean + "\n\n"

            + "USERNAME\n"
            + " • " + clean + "\n\n"

            + "PUBLIC PROFILE URLS\n\n"

            + "Instagram\n"
            + " • https://www.instagram.com/" + clean + "/\n"
            + " • Status: CHECK PUBLICLY\n\n"

            + "X / Twitter\n"
            + " • https://x.com/" + clean + "\n"
            + " • Status: CHECK PUBLICLY\n\n"

            + "Reddit\n"
            + " • https://www.reddit.com/user/" + clean + "/\n"
            + " • Status: CHECK PUBLICLY\n\n"

            + "GitHub\n"
            + " • https://github.com/" + clean + "\n"
            + " • Status: CHECK PUBLICLY\n\n"

            + "TikTok\n"
            + " • https://www.tiktok.com/@" + clean + "\n"
            + " • Status: CHECK PUBLICLY\n\n"

            + "PUBLIC CONTACT INDICATORS\n"
            + " • None automatically extracted\n\n"

            + "ASSESSMENT\n"
            + "Username-based public-source check only.\n\n"

            + "NOTE\n"
            + "A generated URL does NOT confirm that a profile exists.\n"
            + "Results must be verified from the public source.\n"
            + "No login, private-account access, credential collection,\n"
            + "or bypass is performed.";

    resultText.setText(report);
}

}
